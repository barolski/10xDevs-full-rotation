---
import Layout from "@/layouts/Layout.astro";
import SignUpForm from "@/components/auth/SignUpForm";
import { Card, CardContent, CardFooter, CardHeader, CardTitle } from "@/components/ui/card";
import { safeNext, withNext } from "@/lib/redirect";

const error = Astro.url.searchParams.get("error");
const next = safeNext(Astro.url.searchParams.get("next"));
---

<Layout title="Sign up">
  <main class="bg-background flex min-h-screen items-center justify-center p-4">
    <Card className="w-full max-w-sm">
      <CardHeader className="text-center">
        <CardTitle className="text-2xl">
          <h1>Sign up</h1>
        </CardTitle>
      </CardHeader>
      <CardContent>
        <SignUpForm serverError={error} next={next} client:load />
      </CardContent>
      <CardFooter className="justify-center">
        <p class="text-muted-foreground text-sm">
          Already have an account?{" "}
          <a
            href={withNext("/auth/signin", next)}
            class="text-foreground hover:decoration-primary underline underline-offset-4"
          >
            Sign in
          </a>
        </p>
      </CardFooter>
    </Card>
  </main>
</Layout>
